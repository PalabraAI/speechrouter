{
  "forwarding": {
    "streaming": "handshake JSON frame",
    "batch": "`request` JSON part"
  },
  "note": "Audio must be linear16 mono at 16 kHz or 24 kHz (streaming) or a mono 16-bit PCM WAV at one of those rates (batch, max 32 MB / 10 min); nothing is transcoded. `diarization=true` selects DIARIZATION mode and overrides `mode`. The unified `language` hint is translated to a language name and merged into `languageBias`; `keyterms` are merged into `keywords`. `authorization`, `audioEncoding`, `model`, `partialMode` and `emitAudioProgress` are owned by the adapter and ignored here.",
  "params": [
    {
      "name": "mode",
      "type": "string",
      "enum": [
        "ENDPOINTING",
        "PUSH_TO_TALK",
        "DIARIZATION"
      ],
      "default": "ENDPOINTING",
      "modes": [
        "streaming",
        "batch"
      ],
      "doc": "Turn detection. ENDPOINTING emits one final per detected utterance; PUSH_TO_TALK emits a single final when input ends (no turn timestamps); DIARIZATION adds speaker labels"
    },
    {
      "name": "languageBias",
      "type": "array",
      "modes": [
        "streaming",
        "batch"
      ],
      "doc": "Language names to steer recognition toward, e.g. [\"English\", \"French\"]; merged with `language`"
    },
    {
      "name": "keywords",
      "type": "array",
      "modes": [
        "streaming",
        "batch"
      ],
      "doc": "Vocabulary to bias toward (names, jargon, product terms); merged with `keyterms`"
    },
    {
      "name": "zdrOverride",
      "type": "boolean",
      "modes": [
        "streaming"
      ],
      "doc": "Override the account's Zero Data Retention policy for this session: true forces metadata-only logging"
    },
    {
      "name": "sessionId",
      "type": "string",
      "modes": [
        "streaming",
        "batch"
      ],
      "doc": "Correlation id echoed in Meta's server logs; generated upstream when omitted"
    }
  ]
}
